# Fast Fill

Fills an element's height to its parent inner height - cousin objects.

	$('.dom').fill();

	$('.dom').fill({ parent: document.body });

## Options

- parent: element to fill to, defaults to callee's parent.